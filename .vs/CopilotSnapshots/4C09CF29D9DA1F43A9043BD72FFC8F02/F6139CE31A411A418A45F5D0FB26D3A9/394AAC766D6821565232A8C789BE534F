import { Link, useLocation } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'

const navItems = {
  worker: [
    { path: '/worker/dashboard', label: 'Dashboard', icon: '📊' },
    { path: '/worker/profile', label: 'Profile', icon: '👤' },
    { path: '/worker/salary', label: 'Salary', icon: '💰' },
    { path: '/worker/transactions', label: 'Transactions', icon: '💳' },
    { path: '/worker/complaints', label: 'Complaints', icon: '📋' },
    { path: '/worker/learning', label: 'Learning', icon: '📚' },
  ],
  manager: [
    { path: '/manager/dashboard', label: 'Dashboard', icon: '📊' },
    { path: '/manager/workers', label: 'Workers', icon: '👥' },
    { path: '/manager/payments', label: 'Payments', icon: '💰' },
    { path: '/manager/reports', label: 'Reports', icon: '📈' },
    { path: '/manager/complaints', label: 'Complaints', icon: '📋' },
  ],
  owner: [
    { path: '/owner/dashboard', label: 'Dashboard', icon: '📊' },
    { path: '/owner/analytics', label: 'Analytics', icon: '📈' },
    { path: '/owner/expenses', label: 'Expenses', icon: '🏗️' },
    { path: '/owner/revenue', label: 'Revenue', icon: '💵' },
    { path: '/owner/reports', label: 'Reports', icon: '📑' },
  ],
  labor: [
    { path: '/labor/dashboard', label: 'Dashboard', icon: '📊' },
    { path: '/labor/cases', label: 'Cases', icon: '📁' },
    { path: '/labor/complaints', label: 'Complaints', icon: '📋' },
    { path: '/labor/resolutions', label: 'Resolutions', icon: '✅' },
  ],
  hr: [
    { path: '/hr/dashboard', label: 'Dashboard', icon: '📊' },
    { path: '/hr/salary-credit', label: 'Credit Salary', icon: '💰' },
    { path: '/hr/loans', label: 'Loans', icon: '🏦' },
  ],
}

export default function Sidebar() {
  const { profile, signOut } = useAuth()
  const location = useLocation()
  const role = profile?.role || 'worker'
  const items = navItems[role] || []

  return (
    <aside className="w-64 bg-gray-900 text-white min-h-screen flex flex-col">
      <div className="p-6 border-b border-gray-700">
        <h1 className="text-lg font-bold">Financial Dashboard</h1>
        <p className="text-sm text-gray-400 mt-1 capitalize">{role} Portal</p>
      </div>
      <nav className="flex-1 p-4 space-y-1">
        {items.map((item) => (
          <Link
            key={item.path}
            to={item.path}
            className={`flex items-center gap-3 px-4 py-2.5 rounded-lg transition-colors ${
              location.pathname === item.path
                ? 'bg-blue-600 text-white'
                : 'text-gray-300 hover:bg-gray-800'
            }`}
          >
            <span>{item.icon}</span>
            <span>{item.label}</span>
          </Link>
        ))}
      </nav>
      <div className="p-4 border-t border-gray-700">
        <div className="text-sm text-gray-400 mb-2">{profile?.full_name}</div>
        <button
          onClick={signOut}
          className="w-full px-4 py-2 bg-red-600 hover:bg-red-700 rounded-lg text-sm transition-colors"
        >
          Sign Out
        </button>
      </div>
    </aside>
  )
}
